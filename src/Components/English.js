import React, { Component } from 'react';

export default class English extends Component {
    constructor(props) {
        super(props);
    }

    render() {
        return (
            <div id="calendar">
                english page
            </div>
        )
    }
}